__pycache__/
*.pyc
.env
*.log
.DS_Store
Thumbs.db

# Local pipeline checkpoints/caches (not for deploy)
.remediate_done.txt
generate/.community_cache/
.deslop_done.txt

# Workspace memory dir (sensitive: API tokens, user prefs) — never deploy
.workbuddy/